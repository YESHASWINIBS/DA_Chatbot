Project: AI Gateway with Smart Routing

This project implements an AI Gateway that intelligently routes user prompts to different LLMs based on task complexity.

It balances cost, speed, and quality by using a routing model, cache layer, and logging system.

AI Gateway Routing System

This project implements a Smart AI Gateway that routes user prompts based on complexity.

Features:
- Routing model
- Cache
- Logging
- PoC evaluation

Models:
- FAST model → simple queries
- CAPABLE model → complex queries